# DO NOT EDIT - Generated by RSF

variable "aws_region" {
  description = "AWS region for deployment"
  type        = string
  default     = "<< aws_region >>"
}

variable "name_prefix" {
  description = "Prefix for all resource names"
  type        = string
  default     = "<< name_prefix >>"

  validation {
    condition     = can(regex("^[a-zA-Z][a-zA-Z0-9_-]*$", var.name_prefix))
    error_message = "name_prefix must start with a letter and contain only letters, digits, underscores, or hyphens."
  }
}

variable "workflow_name" {
  description = "Workflow name used in resource naming"
  type        = string
  default     = "<< workflow_name >>"
}

variable "timeout" {
  description = "Lambda function timeout in seconds"
  type        = number
  default     = 900
}

variable "memory_size" {
  description = "Lambda function memory in MB"
  type        = number
  default     = 256
}

variable "source_dir" {
  description = "Path to the project source directory"
  type        = string
  default     = ".."
}

variable "log_retention_days" {
  description = "CloudWatch log retention in days"
  type        = number
  default     = 14
}

variable "execution_timeout" {
  description = "Durable execution timeout in seconds (1 to 31622400)"
  type        = number
  default     = 86400

  validation {
    condition     = var.execution_timeout >= 1 && var.execution_timeout <= 31622400
    error_message = "execution_timeout must be between 1 and 31622400 seconds."
  }
}

variable "retention_period" {
  description = "Durable execution retention period in days (1 to 90)"
  type        = number
  default     = 14

  validation {
    condition     = var.retention_period >= 1 && var.retention_period <= 90
    error_message = "retention_period must be between 1 and 90 days."
  }
}
