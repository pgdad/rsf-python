# DO NOT EDIT - Generated by RSF
<% if backend_bucket %>

terraform {
  backend "s3" {
    bucket = "<< backend_bucket >>"
    key    = "<< backend_key >>"
    region = "<< aws_region >>"
<% if backend_dynamodb_table %>
    dynamodb_table = "<< backend_dynamodb_table >>"
<% endif %>
    encrypt = true
  }
}
<% else %>

# No remote backend configured. State is stored locally.
# To enable S3 backend, configure backend_bucket in your RSF project.
<% endif %>
