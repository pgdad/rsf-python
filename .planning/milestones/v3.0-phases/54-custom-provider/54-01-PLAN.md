---
phase: 54-custom-provider
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/rsf/dsl/models.py
  - tests/test_providers/test_custom_config.py
autonomous: true
requirements:
  - CUST-01

must_haves:
  truths:
    - "CustomProviderConfig Pydantic model exists with program (str), args (list[str]), teardown_args (list[str] | None), metadata_transport (Literal['file','env','args']), and env (dict[str,str] | None) fields"
    - "InfrastructureConfig.custom field is typed as CustomProviderConfig | None (not dict[str, Any])"
    - "CustomProviderConfig uses model_config extra='forbid' matching project convention"
    - "metadata_transport defaults to 'file' when not specified"
    - "args defaults to empty list when not specified"
    - "Pydantic validation rejects invalid metadata_transport values"
  artifacts:
    - path: "src/rsf/dsl/models.py"
      provides: "CustomProviderConfig Pydantic model and updated InfrastructureConfig.custom type"
      exports: ["CustomProviderConfig"]
    - path: "tests/test_providers/test_custom_config.py"
      provides: "Tests for CustomProviderConfig validation"
  key_links:
    - from: "src/rsf/dsl/models.py"
      to: "src/rsf/dsl/models.py"
      via: "CustomProviderConfig referenced by InfrastructureConfig.custom"
      pattern: "custom: CustomProviderConfig \\| None"
---

<objective>
Create the CustomProviderConfig Pydantic model and replace the dict[str, Any] placeholder in InfrastructureConfig with the typed model.

Purpose: CUST-01 (configure arbitrary program as infrastructure provider). This plan creates the configuration foundation that Plan 02 consumes.
Output: Typed config model with validation, comprehensive tests.
</objective>

<execution_context>
@/home/esa/.claude/get-shit-done/workflows/execute-plan.md
@/home/esa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/rsf/dsl/models.py
@.planning/phases/54-custom-provider/54-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/rsf/dsl/models.py (current state):
```python
class TerraformProviderConfig(BaseModel):
    """Terraform-specific provider configuration."""
    model_config = {"extra": "forbid", "populate_by_name": True}
    tf_dir: str = "terraform"
    backend_bucket: str | None = None
    backend_key: str | None = None
    backend_dynamodb_table: str | None = None

class InfrastructureConfig(BaseModel):
    """Infrastructure provider configuration block."""
    model_config = {"extra": "forbid", "populate_by_name": True}
    provider: str = "terraform"
    terraform: TerraformProviderConfig | None = None
    cdk: dict[str, Any] | None = None  # Phase 53
    custom: dict[str, Any] | None = None  # Phase 54 -- REPLACE THIS
```

Note: the `cdk` field is also `dict[str, Any]` -- do NOT change it, that is Phase 53's concern. Only change `custom`.
</interfaces>
</context>

<tasks>

<feature>
  <name>CustomProviderConfig Pydantic model and InfrastructureConfig update</name>
  <files>
    src/rsf/dsl/models.py, tests/test_providers/test_custom_config.py
  </files>
  <behavior>
    CustomProviderConfig Pydantic model (in src/rsf/dsl/models.py, defined BEFORE InfrastructureConfig):
    - model_config = {"extra": "forbid", "populate_by_name": True} (matching TerraformProviderConfig convention)
    - program: str -- absolute path to the executable (required, no default)
    - args: list[str] = [] -- arg templates with {placeholder} substitution (e.g., ["deploy", "--env", "{stage}"])
    - teardown_args: list[str] | None = None -- optional separate teardown arg templates
    - metadata_transport: Literal["file", "env", "args"] = "file" -- which transport mechanism to use
    - env: dict[str, str] | None = None -- optional extra environment variables for the subprocess
    - timeout: int | None = None -- optional timeout in seconds (None means no timeout)

    InfrastructureConfig update:
    - Change `custom: dict[str, Any] | None = None` to `custom: CustomProviderConfig | None = None`
    - Do NOT change any other fields (cdk remains dict[str, Any])

    Test cases (tests/test_providers/test_custom_config.py):
    - test_minimal_config_requires_program: CustomProviderConfig(program="/usr/bin/deploy") succeeds
    - test_program_is_required: CustomProviderConfig() without program raises ValidationError
    - test_args_defaults_to_empty_list: config without args has args == []
    - test_metadata_transport_defaults_to_file: config without metadata_transport has metadata_transport == "file"
    - test_metadata_transport_accepts_file: metadata_transport="file" is valid
    - test_metadata_transport_accepts_env: metadata_transport="env" is valid
    - test_metadata_transport_accepts_args: metadata_transport="args" is valid
    - test_metadata_transport_rejects_invalid: metadata_transport="invalid" raises ValidationError
    - test_teardown_args_defaults_to_none: config without teardown_args has teardown_args is None
    - test_env_defaults_to_none: config without env has env is None
    - test_timeout_defaults_to_none: config without timeout has timeout is None
    - test_extra_fields_rejected: extra="forbid" rejects unknown fields
    - test_full_config: config with all fields populated works correctly
    - test_infrastructure_config_accepts_custom: InfrastructureConfig(provider="custom", custom=CustomProviderConfig(program="/usr/bin/deploy")) works
    - test_infrastructure_config_custom_none_by_default: InfrastructureConfig().custom is None
    - test_infrastructure_config_yaml_round_trip: dict with nested custom block validates to proper types
  </behavior>
  <implementation>
    1. Add `from typing import Literal` import if not already present in models.py (check first -- it may already be imported)
    2. Define CustomProviderConfig class BEFORE InfrastructureConfig in models.py (since InfrastructureConfig references it)
    3. Place it AFTER TerraformProviderConfig (alphabetical convention)
    4. Update InfrastructureConfig.custom type annotation from dict[str, Any] | None to CustomProviderConfig | None
    5. Remove the "# Phase 54" comment on the custom field since it is now implemented
    6. Create test file with all test cases
    7. Run pytest tests/test_providers/test_custom_config.py -x to verify all pass
  </implementation>
</feature>

</tasks>

<verification>
- `pytest tests/test_providers/test_custom_config.py -x` passes all tests
- `python -c "from rsf.dsl.models import CustomProviderConfig; c = CustomProviderConfig(program='/bin/sh'); assert c.metadata_transport == 'file'"` succeeds
- `python -c "from rsf.dsl.models import InfrastructureConfig; c = InfrastructureConfig(provider='custom'); assert c.custom is None"` succeeds
- Existing tests still pass: `pytest tests/ -x --ignore=tests/test_providers/test_custom_config.py -q` (spot check)
</verification>

<success_criteria>
- CustomProviderConfig Pydantic model has all fields with correct types and defaults
- InfrastructureConfig.custom is typed as CustomProviderConfig | None (not dict[str, Any])
- Pydantic validation rejects invalid metadata_transport values
- Extra fields are rejected (extra="forbid")
- All test cases pass
- Existing tests are not broken
</success_criteria>

<output>
After completion, create `.planning/phases/54-custom-provider/54-01-SUMMARY.md`
</output>
