---
phase: 51-provider-interface-and-metadata-foundation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/rsf/providers/__init__.py
  - src/rsf/providers/base.py
  - src/rsf/providers/registry.py
  - tests/test_providers/__init__.py
  - tests/test_providers/test_base.py
  - tests/test_providers/test_registry.py
autonomous: true
requirements: [PROV-01, PROV-02, PROV-03, PROV-04]

must_haves:
  truths:
    - "Instantiating a class that omits any abstract method raises TypeError"
    - "All abstract methods receive a single ProviderContext argument"
    - "run_provider_command() executes subprocess with shell=False"
    - "Provider registry resolves name to implementation"
    - "check_prerequisites() returns list[PrerequisiteCheck] with name, status, message"
  artifacts:
    - path: "src/rsf/providers/__init__.py"
      provides: "Package exports"
      exports: ["InfrastructureProvider", "ProviderContext", "PrerequisiteCheck", "get_provider", "register_provider"]
    - path: "src/rsf/providers/base.py"
      provides: "ABC + ProviderContext + PrerequisiteCheck + run_provider_command"
      min_lines: 60
    - path: "src/rsf/providers/registry.py"
      provides: "Dict-dispatch provider registry"
      exports: ["register_provider", "get_provider"]
    - path: "tests/test_providers/test_base.py"
      provides: "ABC contract tests"
      min_lines: 50
    - path: "tests/test_providers/test_registry.py"
      provides: "Registry tests"
      min_lines: 30
  key_links:
    - from: "src/rsf/providers/registry.py"
      to: "src/rsf/providers/base.py"
      via: "import InfrastructureProvider"
      pattern: "from rsf\\.providers\\.base import"
    - from: "src/rsf/providers/__init__.py"
      to: "src/rsf/providers/base.py"
      via: "re-exports"
      pattern: "from rsf\\.providers\\.base import"
---

<objective>
Create the InfrastructureProvider ABC, ProviderContext/PrerequisiteCheck dataclasses, run_provider_command() concrete method, and provider registry.

Purpose: Establishes the provider contract that all downstream providers (Terraform, CDK, Custom) implement against. This is the foundation for the entire v3.0 pluggable infrastructure system.
Output: `src/rsf/providers/` package with base.py (ABC + dataclasses) and registry.py (dict-dispatch factory)
</objective>

<execution_context>
@/home/esa/.claude/get-shit-done/workflows/execute-plan.md
@/home/esa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-provider-interface-and-metadata-foundation/51-RESEARCH.md
@.planning/phases/51-provider-interface-and-metadata-foundation/51-CONTEXT.md

<interfaces>
<!-- Existing CheckResult in doctor_cmd.py — PrerequisiteCheck should align with this pattern -->
From src/rsf/cli/doctor_cmd.py:
```python
CheckStatus = Literal["PASS", "WARN", "FAIL"]

@dataclass
class CheckResult:
    name: str
    status: CheckStatus
    version: str | None = None
    message: str = ""
    fix_hint: str | None = None
```

<!-- deploy_cmd.py subprocess usage pattern -->
From src/rsf/cli/deploy_cmd.py:
```python
subprocess.run(["terraform", "init"], cwd=tf_dir, check=True)
subprocess.run(apply_cmd, cwd=tf_dir, check=True)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create InfrastructureProvider ABC with ProviderContext and PrerequisiteCheck</name>
  <files>src/rsf/providers/__init__.py, src/rsf/providers/base.py, tests/test_providers/__init__.py, tests/test_providers/test_base.py</files>
  <behavior>
    - Test: Subclass that implements all 5 abstract methods instantiates successfully
    - Test: Subclass that omits `deploy()` raises `TypeError` on instantiation
    - Test: Subclass that omits `generate()` raises `TypeError` on instantiation
    - Test: Subclass that omits `teardown()` raises `TypeError` on instantiation
    - Test: Subclass that omits `check_prerequisites()` raises `TypeError` on instantiation
    - Test: Subclass that omits `validate_config()` raises `TypeError` on instantiation
    - Test: `ProviderContext` fields: metadata (WorkflowMetadata — use a mock/stub for now), output_dir (Path), stage (str | None), workflow_path (Path), definition (Any | None, default None)
    - Test: `PrerequisiteCheck` has fields: name (str), status (Literal["pass", "warn", "fail"]), message (str)
    - Test: `run_provider_command()` calls subprocess.run with shell=False, returns CompletedProcess
    - Test: `run_provider_command()` raises subprocess.CalledProcessError on non-zero exit
    - Test: `run_provider_command()` merges provided env dict with os.environ
  </behavior>
  <action>
Create `src/rsf/providers/` package:

1. Create `src/rsf/providers/__init__.py` — re-export `InfrastructureProvider`, `ProviderContext`, `PrerequisiteCheck` from base.py, and `register_provider`, `get_provider` from registry.py.

2. Create `src/rsf/providers/base.py`:
   - Import `abc.ABC`, `abc.abstractmethod`, `dataclasses.dataclass`, `subprocess`, `os`, `logging`, `pathlib.Path`, `typing.Literal`, `typing.Any`
   - Define `PrerequisiteCheck` as a `@dataclass` with fields: `name: str`, `status: Literal["pass", "warn", "fail"]`, `message: str`
   - Define `ProviderContext` as a `@dataclass` with fields: `metadata: Any` (will be WorkflowMetadata once Plan 02 creates it — use `Any` for now to avoid circular imports), `output_dir: Path`, `stage: str | None`, `workflow_path: Path`, `definition: Any = None`
   - Define `InfrastructureProvider(ABC)` with:
     - `@abstractmethod def generate(self, ctx: ProviderContext) -> None`
     - `@abstractmethod def deploy(self, ctx: ProviderContext) -> None`
     - `@abstractmethod def teardown(self, ctx: ProviderContext) -> None`
     - `@abstractmethod def check_prerequisites(self, ctx: ProviderContext) -> list[PrerequisiteCheck]`
     - `@abstractmethod def validate_config(self, ctx: ProviderContext) -> None`
     - Concrete method `run_provider_command(self, cmd: list[str], cwd: Path | None = None, env: dict[str, str] | None = None) -> subprocess.CompletedProcess[str]`: merges `env` with `os.environ`, calls `subprocess.run(cmd, cwd=cwd, env=merged_env, check=True, capture_output=True, text=True, shell=False)`, logs the command with `logger.info("Running: %s", " ".join(cmd))`, returns result.

3. Create `tests/test_providers/__init__.py` (empty).

4. Create `tests/test_providers/test_base.py` with TDD tests for all behaviors listed above. Use `unittest.mock.patch` for subprocess.run in run_provider_command tests. For ProviderContext metadata field, use a simple dataclass stub.

Run RED phase: `pytest tests/test_providers/test_base.py -x` — all tests must fail.
Implement production code.
Run GREEN phase: `pytest tests/test_providers/test_base.py -x` — all tests must pass.
  </action>
  <verify>
    <automated>pytest tests/test_providers/test_base.py -x -v</automated>
  </verify>
  <done>
    - InfrastructureProvider ABC exists with 5 abstract methods
    - Omitting any abstract method raises TypeError
    - ProviderContext dataclass has metadata, output_dir, stage, workflow_path, definition fields
    - PrerequisiteCheck dataclass has name, status, message fields
    - run_provider_command() works with shell=False, env merging, error propagation
    - All tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create provider registry with dict-dispatch</name>
  <files>src/rsf/providers/registry.py, tests/test_providers/test_registry.py</files>
  <behavior>
    - Test: `register_provider("test", TestProvider)` registers successfully
    - Test: `get_provider("test")` returns an instance of TestProvider
    - Test: `get_provider("unknown")` raises `ProviderNotFoundError`
    - Test: `list_providers()` returns names of all registered providers
    - Test: Registering same name twice overwrites (last-write-wins)
    - Test: `ProviderNotFoundError` is a subclass of `RSFError` or `KeyError` (use KeyError for simplicity)
  </behavior>
  <action>
1. Create `src/rsf/providers/registry.py`:
   - Module-level `_PROVIDERS: dict[str, type[InfrastructureProvider]] = {}`
   - `def register_provider(name: str, cls: type[InfrastructureProvider]) -> None:` — stores `cls` in `_PROVIDERS[name]`
   - `def get_provider(name: str) -> InfrastructureProvider:` — looks up name in `_PROVIDERS`, raises `ProviderNotFoundError(f"Unknown provider: {name}. Available: {list(_PROVIDERS.keys())}")` if not found, otherwise returns `_PROVIDERS[name]()`
   - `def list_providers() -> list[str]:` — returns `sorted(_PROVIDERS.keys())`
   - `class ProviderNotFoundError(KeyError): pass`

2. Create `tests/test_providers/test_registry.py` with TDD tests. Create a minimal concrete provider subclass (`DummyProvider`) in the test file that implements all 5 abstract methods as no-ops.

3. Update `src/rsf/providers/__init__.py` to also export `register_provider`, `get_provider`, `list_providers`, `ProviderNotFoundError`.

Run RED → GREEN cycle.
  </action>
  <verify>
    <automated>pytest tests/test_providers/test_registry.py -x -v</automated>
  </verify>
  <done>
    - register_provider() stores provider class by name
    - get_provider() returns instantiated provider
    - get_provider() raises ProviderNotFoundError for unknown names
    - list_providers() returns sorted list of registered names
    - All tests pass
  </done>
</task>

</tasks>

<verification>
```bash
# Run all provider tests
pytest tests/test_providers/ -x -v

# Verify ABC contract — this Python snippet must raise TypeError
python -c "
from rsf.providers.base import InfrastructureProvider
class Bad(InfrastructureProvider):
    pass
Bad()
" 2>&1 | grep -q "TypeError" && echo "ABC contract enforced" || echo "FAIL: ABC not enforced"

# Verify imports work
python -c "from rsf.providers import InfrastructureProvider, ProviderContext, PrerequisiteCheck, get_provider, register_provider, list_providers, ProviderNotFoundError; print('All imports OK')"
```
</verification>

<success_criteria>
- InfrastructureProvider ABC enforces all 5 abstract methods at instantiation
- ProviderContext and PrerequisiteCheck dataclasses exist with correct fields
- run_provider_command() is a concrete method with shell=False, env merging, logging
- Provider registry supports register, get, list operations
- All tests in tests/test_providers/ pass
- Package exports all public types from __init__.py
</success_criteria>

<output>
After completion, create `.planning/phases/51-provider-interface-and-metadata-foundation/51-01-SUMMARY.md`
</output>
