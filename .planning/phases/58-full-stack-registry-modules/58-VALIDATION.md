---
phase: 58
slug: full-stack-registry-modules
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-04
---

# Phase 58 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Terraform validate/plan (no pytest — Phase 59 handles automated tests) |
| **Config file** | examples/registry-modules-demo/terraform/*.tf |
| **Quick run command** | `cd examples/registry-modules-demo/terraform && terraform validate` |
| **Full suite command** | `cd examples/registry-modules-demo && bash deploy.sh deploy` + AWS Console verification |
| **Estimated runtime** | ~5 seconds (validate), ~120 seconds (full deploy) |

---

## Sampling Rate

- **After every task commit:** Run `cd examples/registry-modules-demo/terraform && terraform init -backend=false && terraform validate`
- **After every plan wave:** Run `terraform plan -var-file=terraform.tfvars.json` with sample tfvars
- **Before `/gsd:verify-work`:** Full deploy + AWS Console verification of all five resource types
- **Max feedback latency:** 5 seconds (validate)

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 58-01-01 | 01 | 1 | REG-02 | smoke | `terraform validate` | ❌ W0 | ⬜ pending |
| 58-01-02 | 01 | 1 | REG-03 | smoke | `terraform validate` | ❌ W0 | ⬜ pending |
| 58-02-01 | 02 | 1 | REG-04 | smoke | `terraform validate` | ❌ W0 | ⬜ pending |
| 58-02-02 | 02 | 1 | REG-05 | smoke | `terraform validate` | ❌ W0 | ⬜ pending |
| 58-03-01 | 03 | 2 | all | integration | `bash deploy.sh deploy` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `examples/registry-modules-demo/.gitignore` — add `terraform.tfvars.json` entry
- [ ] Sample terraform.tfvars.json for validation — generated by deploy.sh, but needed for terraform validate/plan

*Existing infrastructure covers test framework — no new test framework needed.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| DynamoDB table exists in AWS | REG-02 | Requires live AWS | Console → DynamoDB → verify table name matches workflow |
| SQS DLQ exists with correct config | REG-03 | Requires live AWS | Console → SQS → verify queue name, retention, redrive policy |
| CloudWatch alarms fire correctly | REG-04 | Requires live AWS + Lambda errors | Console → CloudWatch → verify 3 alarms with correct metrics |
| SNS topic wired to alarms | REG-05 | Requires live AWS | Console → SNS → verify topic ARN matches alarm_actions |
| All resources created by single rsf deploy | SC-5 | Full end-to-end | `rsf deploy` → Console → verify all 5 resource types |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 5s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
